#ifndef _SORT_H
#define _SORT_H
#include <stdlib.h>
#include <stdio.h>
#include <limits.h>

void print_array(const int *array, size_t size);
void radix_sort(int *array, size_t size);

#endif /* _SORT_H */
